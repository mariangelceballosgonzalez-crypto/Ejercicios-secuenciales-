Algoritmo InvertirNumero
	Definir num, decena, unidad, invertido Como Entero
    Escribir "Ingrese un numero de dos cifras:"
    Leer num
    decena <- num / 10
    unidad <- num MOD 10
    invertido <- (unidad * 10) + decena
    Escribir "El numero invertido es: ", invertido
	FinProceso 
